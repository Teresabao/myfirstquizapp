import { Request, Response } from 'express';

class QuizletController {
    createDeck(req: Request, res: Response) {
        // Logic to create a new deck
    }

    getDecks(req: Request, res: Response) {
        // Logic to retrieve all decks
    }

    addFlashcard(req: Request, res: Response) {
        // Logic to add a flashcard to a deck
    }
}

export default QuizletController;
